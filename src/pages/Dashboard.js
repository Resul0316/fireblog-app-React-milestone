import React from 'react'
import { useAuthContext } from '../contexts/AuthContext'

const Dashboard = () => {
  //const {state, setState}  = useAuthContext()

  return (
    <div>
       {/* {state} */}
      {/* <button onClick={() =>setState('merhaba dunya') }>Submit</button> */}
    </div>
  )
}

export default Dashboard